<!DOCTYPE html>
<html>
<head>
  <title>Bot Dashboard</title>
</head>
<body>
  <h1>Danh sách chờ</h1>
  <% if (queue.length > 0) { %>
    <ul>
      <% queue.forEach(song => { %>
        <li><%= song.title %></li>
      <% }); %>
    </ul>
  <% } else { %>
    <p>Không có bài hát trong danh sách chờ.</p>
  <% } %>
</body>
</html>
